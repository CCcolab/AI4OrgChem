> **公开版说明（2026-08-26）：** 本文是命题关闭时形成的冻结证据快照。其中“未启动/禁用”等阶段性措辞只描述当时的任务边界，不代表项目当前进度；当前总状态以 `project/P01-P14_MASTER_TABLE_zh-CN.md` 和 `ai4s-agent/EVALUATION_SUMMARY.json` 为准。

# P14 应变芳香π-distortivity协议 v0.1

- 协议ID：`p14-strained-aromatic-pi-distortivity/0.1`
- 日期：2026-08-22
- 状态：源定义已冻结；v0.3.1已修正证据资格门禁；B3LYPG/6-31G(d)生产优化通过
- 原著程序代码：不使用

## 唯一命题

在原著10-12号benzotricyclobutadiene（C12H6）和冻结的PLG定义下，中央环显著键长交替是否主要由中央环A与外围三个C=C片段B之间的π作用引起，而不能只归因于小环角应变。

本协议不检验原著19分子普适性，不增加杂原子，不宣称所有应变芳香体系均遵循相同方向。

## 体系与片段

- 分子：benzotricyclobutadiene，原著编号10-12，`C12H6`；
- 电荷/多重度：0/1；气相；闭壳层；
- A：中央六元环`C1–C6`；
- B：外围三个双键`C7=C8`、`C9=C10`、`C11=C12`；
- 结构对称性：首轮限定平面D3h五参数模型`(rendo,rexo,rside,router,rCH)`。

## 电子态

- `G`：普通RKS电子态；
- `PLG`：A/B间π-AO的Fock与overlap矩阵元为零，并应用P09已测试的15类2011交换积分删除；
- 不重新开发LFMO或新的积分分类，复用已经通过测试的P09 AO分类和交换积分谓词。

## 计算顺序

1. STO-3G固定源几何技术烟测：检查78电子、SCF收敛、A/B π块为零、15类交换删除及能量分量闭合；不作科学判定。
2. 普通B3LYPG/6-31G(d)五参数D3h G生产优化：已通过，最大梯度`0.00146286 Eh/Å`。
3. 条件B3LYPG/6-31G(d)五参数D3h PLG生产优化：已通过，最大梯度`0.00116557 Eh/Å`；使用内存受控的积分屏蔽实现。
4. `Δr(G)=0.177240 Å`、`Δr(PLG)=0.005037 Å`、`dΔr(GP)=0.172204 Å`；优化端点`67.679719 kcal/mol`。结构差和能量差均在预注册容差内，P14最终分类恢复为“一致”。

## 资源边界

- WSL可见内存15 GiB，低于4 GiB停止；
- 8 CPU线程，同时只运行1个P14 QM任务；
- 不在6-31G*层级保留物理ERI和掩蔽ERI两份完整四阶张量；
- GPU不参与；scratch上限20 GiB。

## 停止条件

电子数、SCF、幂等性、交叉π块、15类删除或能量闭合任一失败即停止当前分支。不得通过增加分子、扫描基组/泛函或改变PLG定义追逐原著数值。

优化器返回`success=true`不能单独作为收敛依据。科学定判还必须同时满足：记录允许科学分类、生产优化已授权、方法/基组为B3LYPG/6-31G(d)、G与PLG均SCF收敛、78电子、无活动边界、最大梯度不超过预注册阈值，并记录合格终止原因。STO-3G技术烟测不得替代生产优化。

## 输出

最终必须提供输入坐标、四个底层机器JSON、确定性分类JSON、计算报告、专用计算入口、验证器、数据卡和“一致、不一致或部分一致”结论。所有引用文件必须存在并由SHA-256锁定；CI必须从底层记录重新计算BLA、端点能量和判定门禁。生产标签及AI训练保持关闭。
